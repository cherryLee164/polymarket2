# Findings Log

- Review started for the Next.js shell plus `scripts/monitor.js`.
- Existing local change set is not committed yet; review should avoid reverting user work.
- `scripts/monitor.js` reuses the same raw file name for the same event hour and opens it in append mode, which can merge multiple interrupted runs into one file while the summary only reflects the latest process run.
- `scripts/monitor.js` samples from the Gamma `/events?slug=...` endpoint, but runtime checks show that endpoint is CDN-cached (`Cache-Control: public, max-age=120`, `cf-cache-status: HIT`), so a 5-second polling loop can still record 1-2 minute old prices.
- `startEvent()` does not catch transient fetch errors; startup and hour-rollover failures bubble to the top-level `main().catch(...)` and terminate the monitor instead of retrying.
- The Next.js app is still a static placeholder page; there is no API/shared module that exposes collected `data/events` or `data/summaries` to the UI layer yet.
- `npm run lint`, `npm run build`, and `node -c scripts/monitor.js` all pass after the proxy-compatible request fallback change.
- The patched monitor path was validated with a short run and logged `Started ... (market ..., clob-buy)` followed by live CLOB samples such as `Up 43.000c Down 56.000c`.
- An older monitor process is still writing to the legacy raw file name pattern, so the user needs to restart the monitor to switch runtime behavior to the patched code.
- Historical files under `data/events` and `data/summaries` were cleared so future records become the new baseline.
- The summary threshold rule has now been changed from `< threshold` to `<= threshold`.
- The home page now works as a summary list page with date-range filtering, newest-first ordering, and pagination; because data was reset, the summary table remains empty until the first >=50 minute run finishes.
- The user wants trading logic kept separate from monitoring, so the order engine should not be embedded in `scripts/monitor.js`.
- The intended order rule is: first side enters at `<= 35`, opposite side can fill at `<= 40`, no first entry if the first `<= 35` signal appears with `<= 30` minutes left, and the next hour uses `$2` only when the previous qualified hour did not show both sides at `<= 40`.
- Polymarket's official trading flow goes through the CLOB client SDK with authenticated signed orders, not the public price endpoints alone.
- The official CLOB SDK supports `createAndPostMarketOrder`, and for buy orders its `amount` field is the USDC amount, which fits the `$1/$2` strategy directly.
- On this machine the SDK path is viable because it uses `axios`, and local runtime checks showed `axios` can reach Polymarket even though plain Node `fetch` needed the curl proxy fallback.
- The new order engine stores resumable runtime state under `data/orders/runtime-state.json` and per-hour audit logs under `data/orders/logs`, so a restart does not double-buy the same side.
- Carry-over sizing now only uses the immediately previous qualified hour within a one-hour lookback window; if that hour is missing, the engine falls back to the safe base amount `$1`.
- `engine.py` uses Python `py_clob_client`, whose `create_or_derive_api_creds()` really does fall back from create to derive. That behavior is more reliable for this account than the JS client path.
- The live order launcher has now been moved to a Python implementation in `scripts/order.py`, while the older JS `scripts/order.js` remains in the repo only as a reference path.
- A root-level `config.py` is now present in this repo and matches the old engine's auth defaults: `PK`, `SIGNATURE_TYPE = 1`, `FUNDER = PROXY_ADDRESS`, and `HOST = https://clob.polymarket.com`.
- `scripts/order.py` still reads only env aliases today, so adding `config.py` fallback support will remove one more mismatch with the legacy engine.
- Settled hourly BTC markets expose enough data for realized PnL via `outcomes + outcomePrices`; ended samples returned values like `Up=1, Down=0`, so the order engine can reconcile the winning side without hitting private trade history.
- The reporting layer should stay API-shaped instead of UI-shaped. Writing `data/orders/reports/hour-details.json`, `data/orders/reports/order-details.json`, and `data/orders/reports/summary.json` gives the future frontend a stable backend contract while leaving room for 5m/15m/4h expansions later.
- The admin UI should be a single console entry point rather than separate isolated pages. Using top-level view tabs over shared server-side data loaders keeps monitoring and order reporting in one place while preserving room for future multi-timeframe strategy pages.
- A quick threshold backtest over the last 23 settled, >=50 minute monitored hours suggests the current `35/40` rule is not on the local frontier. Under a conservative fill-at-cap model, `35/40` produced about `+$1.21`, while `38/38` produced about `+$4.74`; keeping first fixed at `35`, the best hedge in this sample was closer to `37` than `40`.
- In this sample, widening the hedge threshold beyond the first threshold often did not add more paired hours; it mostly reduced paired-hour payout. For example, with first=`38`, hedge=`38..41` all paired 17 hours, but PnL decayed as the hedge got wider.
- Splitting the sample chronologically still favored tighter/higher thresholds: first-half results were best around `37/38`, and second-half results stayed positive for `38/38` while `35/40` turned negative.
- The 4-hour BTC market uses a different slug family: `btc-updown-4h-<unix_start>`. Local probes against `https://gamma-api.polymarket.com/events?slug=btc-updown-4h-1773662400` showed that the numeric suffix matches the market window start timestamp, which is stable enough to derive the next 4-hour slug automatically.
- `.env.order` currently has live order creds under `PK` / `PORTFOLIO_PRIVATE_KEY` and `CLOB_*`, but it does not yet contain any relayer-claim credentials under `BUILDER_*` or `RELAYER_*`, so relayer-based auto-redeem is not configured yet.
- The custom `.env.order` loader previously treated the first occurrence of a key as final, even if it was blank. That meant placeholder lines could block later real values in the same file until the loader was patched to allow a later non-empty value to replace an earlier empty one.
- The user later added raw relayer key exports under generic names (`apiKey` / `secret` / `passphrase`) rather than the script's expected `BUILDER_*` names. The order script now backfills those aliases automatically before loading legacy config.
- Importing the old `engine.py` directly is not viable in this repo because it hard-imports missing local modules (`scanner`, `executor`). The new auto-redeem path therefore needs to inline the minimum relayer/data-api helpers instead of reusing the old module wholesale.
- The 1-hour and 4-hour monitor variants do not need to be merged into one sampling loop. A safer consolidation point is the launcher: a single `run-monitor.bat` can spawn both stable single-variant monitor processes and prefix their logs, which reduces BAT clutter without destabilizing `scripts/monitor.js`.
- Raw sample pruning inside a run would damage later threshold/backtest analysis. A safer storage control is variant-specific sampling rates plus retention-based cleanup of old raw files and summaries.
- The new BTC short-window markets follow the same timestamp-start slug family as 4H: `btc-updown-15m-<unix_start>` and `btc-updown-5m-<unix_start>`. For these short windows, internal alignment should use integer minutes rather than floating-point hours to avoid slug drift from rounding error.
- The 1H summary gap the user noticed at `2026-03-16 10:00-11:00` was not a UI bug. Local raw files for that hour only covered `44.366` minutes plus another `0.195` minute fragment, so the run stayed below the configured `>= 50 minute` validity threshold and no summary file was written.
- Existing summary JSON files do not contain the new first-hit / sampling-health fields, but the corresponding raw JSONL files are still available. The monitor data loader can backfill those fields on read, which makes the new UI immediately useful on historical samples without waiting for fresh monitor hours.
- Per-variant retention must be applied by file identity, not by the currently running monitor process. Otherwise the combined launcher would let the `5m` process delete older `1h`/`4h` artifacts too aggressively.
- The Polymarket portfolio page's displayed `100美分 / 价值` for a winner position does line up with the data API and live orderbook closely enough to automate exits, but exact `1.0000` is too strict. In live checks, the page rounded `curPrice ≈ 0.9995` and `currentValue ≈ size * 0.9995` to `100美分`, while the actual executable full-size sell price was `0.999`.
- For automatic exits, the safe rule is: use `positions.curPrice/currentValue` to detect a page-equivalent winner, then call `calculate_market_price(..., SELL, full_size, FOK)` and only sell when that executable price is at or above the configured threshold (now defaulted to `99.5c`).
- The user wants settlement unified under `run-order.bat`. A safe compromise is to keep the settlement worker logic in its own script but invoke it from `scripts/order.py` on a scheduler, so operations still run from one BAT while buy logic and settlement logic stay isolated enough to debug.
- Settlement observability belongs in the same admin console as monitoring and orders, but the raw `auto-redeem-log.jsonl` format is too noisy for direct rendering. The useful shape is one compact row per settle cycle: status, sold count, claim attempt/result, redeemable count, balance delta, and a short message.
- The browser fallback logger had a real crash path: `request.post_data` can raise on binary payloads in Playwright. Wrapping that access prevents the listener itself from killing the claim attempt, and the UI can now show meaningful error summaries instead of giant broken traces from a logging bug.
- ESLint was scanning `temp/` reference repos and `data/` runtime artifacts, which polluted lint results with unrelated external errors. Ignoring those paths keeps validation scoped to the actual project code.
- The user no longer wants settlement embedded inside the buy engine. `scripts/order.py` should stop scheduling settlement inline, and `scripts/auto_redeem.py` should become the standalone API settlement daemon behind a dedicated BAT again.
- `5m` markets cannot reuse the hourly calendar slug format. They need the same timestamp-start slug generation used by the monitor path: `btc-updown-5m-<unix_start>` aligned to the 5-minute window start.
- Carry-over sizing must be variant-isolated. A future `5m` run must not inherit sizing from `1h` report/summary history, so carry lookup needs a variant filter.
- A conservative replay over the current `5m` sample set (`222` valid windows) suggests the local frontier is tighter than the hourly strategy. In a fixed `$1 + $1` replay, the best-tested combo was approximately `first=30`, `hedge=40`, with a `1.5` minute late-entry cutoff.
- The safest way to keep improving the monitor UI is to route new monitor rendering through a clean component file. `app/page.js` still contains older mojibake sections, so direct large edits there are brittle.
- The threshold summary cards are most useful when they are computed from the full filtered monitor set before pagination, not from just the current page. The data loader now needs to return both paginated rows and filter-wide aggregates together.
- Re-running the `5m` replay on the expanded sample (`255` valid windows) with flat `$1` sizing and no martingale shifts the local leader from `30/40` toward `30/50` at a `1.5` minute late cutoff. `35`-based entries remain materially weaker on total PnL.
- For this flat-size `5m` path, `30/50` beat `30/40` on both total PnL and max drawdown in the current sample, while `35/40` traded more often and had a shorter losing streak but less than half the total profit.
- `30/45` is not on the current local frontier for `5m` flat-size trading. It pairs more often than `30/40`, but the extra hedge cost cuts total profit enough that both `30/40` and `30/50` still finish ahead in the current replay.
- Settlement logs are only trustworthy to operators when they show actual settlement actions. Startup scans and pending cycles should stay out of the visible admin table; the meaningful row is the completed sell/claim event with price, shares, proceeds, and balance delta.
- The existing live `.env.order` keeps global `ORDER_FIRST_ENTRY_CENTS=38`, `ORDER_HEDGE_ENTRY_CENTS=38`, and `ORDER_SAMPLE_INTERVAL_MS=5000`. A dormant `5M` path therefore cannot safely rely on generic `ORDER_*` keys; it needs variant-scoped defaults or explicit `ORDER_5M_*` overrides.
- A practical `5M` risk brake can be implemented entirely from local order reports. `data/orders/reports/hour-details.json` already contains per-hour `settlementStatus`, `variant`, `eventEnd`, and `netPnlUsd`, which is enough to compute rolling realized-style strategy PnL without reading raw monitor files again.
- To avoid pathological re-pausing on the same bad window after the cooldown expires, the pause state needs to remember the latest triggering `hourKey`; otherwise the engine would restart and immediately pause again with no new settled information.
- If `1H` and `5M` are launched together from one BAT, the previous shared `data/orders/runtime-state.json` becomes a correctness bug: whichever process writes last overwrites the other variant's active state. Runtime state must therefore be variant-scoped.
- Profit splitting belongs in the data layer, not the page. Deriving `summary.byVariant` from `hour-details.json` and `order-details.json` keeps the UI simple and preserves one shared order ledger while still exposing separate `1H` / `5M` PnL cards.
- Launching `1H` and `5M` from one BAT is only safe if the supervisor does not treat one child exit as fatal for the whole launcher. Otherwise a `5M` crash would silently take `1H` down with it, which violates the user's “同一个 BAT，但互不影响” requirement.
- In the live `5M` console output, `PolyApiException[400, order couldn't be fully filled. FOK orders are fully filled or killed.]` is an expected “missed fill” condition rather than a strategy bug. It means there was not enough liquidity to buy the full `$1` at or below the configured threshold.
- The observed `~2.7s` sample spacing on a nominal `2000ms` loop was caused by fixed sleeping after the whole cycle. To hold the intended cadence, the loop needs elapsed-time compensation (`sleep = interval - work_time`) instead of unconditional `time.sleep(interval)`.
- A `5M` restart rule cannot be implemented only on “new event” creation. If a same-window runtime state exists but no order has been placed yet, resuming it after the first 30 seconds would still violate the user's “重启后当前 5 分钟窗口不再参与” expectation. That resume path needs the same gate.
- The user's live `5M` "重复下单" report was real at the attempt layer: `runtime-state-5m.json` showed `attemptCount=4` on the same `up` side within one event, and the paired JSONL log contained repeated `order-transient-error` rows for the same `hourKey`.
- The duplicate-risk root cause was twofold: `5M` still inherited the general cooldown retry path and also performed an immediate second submission inside `place_side_order()` after transient request exceptions.
- The safe `5M` rule is: submit once, then verify. After any failed response, check whether the token position size actually increased; if it did, treat the order as filled, and if it did not, block that side for the rest of the current `5M` event and wait for the next window.
- The settlement worker can look "stuck" even when healthy if an older process is still running or if the console lacks per-cycle scan lines. A startup banner plus explicit scan summaries, combined with `python -u`, makes it much easier to distinguish "no sell candidate" from a real freeze.
- The latest `5M` miss was not because the `50c` hedge condition failed. In `btc-updown-5m-1773759000`, the first `Up` leg filled at `30c`, `Down` later hit `45c`, but the hedge submission raised a transient request exception. Because the prior safety patch hard-blocked the rest of the event after any `5M` failure, later qualifying samples in the same window were ignored.
- A second `5M` guard was still too slow after the previous fix: `ORDER_ATTEMPT_COOLDOWN_MS` remained a global `15s`, so even though `5M` was allowed one more retry in theory, that retry often could not happen before the 5-minute window ended. Making the cooldown variant-scoped and setting `5M=4s` aligns retry timing with the intended execution rhythm.
- The current live `5M` complaint is not a threshold-mixing issue. Local config probes still resolve `5M` to `first=30c`, `hedge=50c`, `sample=2000ms`, `submitGap=4000ms`, while `1H` stays `38/38/5000ms`.
- The real execution gap is that `5M` order submission was still synchronous inside `record_sample()`. When `trader.place_buy()` hit a slow request or `Request exception`, the whole `5M` sample loop paused, so later console rows made it look like the bot saw `29c` and only reacted after the market had already moved much lower.
- Fix applied in `scripts/order.py`:
  - Added `submissionPending` state alongside the existing delayed-confirmation state.
  - `5M` live buys now submit through a single background executor instead of blocking the sample loop.
  - Each sample now polls in-flight submissions first, so success/failure is processed on the next sample without freezing `5M` logging.
  - While a submit is still running, the loop can already detect a position-size increase and mark the side filled before the request returns.
  - `summarize_state()` now shows `submitting:up/down` and `confirming:up/down`, so the console reflects the true phase instead of a misleading plain `waiting`.
- The current `PolyApiException[status_code=None, error_message=Request exception!]` is emitted by `py_clob_client.http_helpers.helpers.request()`, which swallows the original `httpx.RequestError` details and only reports a generic transport failure.
- Read-only inspection of the installed client showed it uses a shared `httpx.Client(http2=True)` without exposing request details; this is a good fit for the intermittent Windows/proxy-like environment issue the user is seeing on live `5M` order POSTs.
- Fix applied in `scripts/order.py`:
  - Reconfigured the CLOB transport from the app side so `ORDER_CLOB_HTTP2=false` by default, with explicit connect/read timeout settings.
  - Monkeypatched the client helper to preserve the underlying `httpx` exception type/message in `PolyApiException`, so future logs show the real transport failure instead of a bare `Request exception!`.
- Settlement scope findings changed again:
  - `scripts/auto_redeem.py` previously treated `ORDER_AUTO_REDEEM_TRACKED_ONLY=false` as "all strategy prefixes", not "all account redeemables".
  - The live account currently reports about `800` redeemable positions when scanned without tracked-only filtering.
  - The worker also only claimed one entry per cycle, which is too slow for a large backlog at a `5m` cadence.
- Fix applied in `scripts/auto_redeem.py`:
  - `ORDER_AUTO_REDEEM_TRACKED_ONLY` now defaults to `false` and truly means "all redeemable positions".
  - Added `ORDER_SETTLEMENT_MAX_CLAIMS_PER_RUN` with `0 = no explicit per-cycle cap`; current `.env.order` is set to `0`.
  - Cycle summaries now record `claimedCount`, `claimErrorCount`, and a preview list of claim results.
- Operational direction changed again after user review:
  - The full-account `800+` redeemable scan is not acceptable because most of those positions are stale/expired history the user does not want touched.
  - Settlement has therefore been moved back to tracked-only mode in `.env.order`, while keeping the code path capable of broader scans if explicitly re-enabled later.
  - The user does not want the `5M` worker killed by the new failure guard; they want a persistent flag so the process can stay alive and keep sampling while the order branches are skipped.
- Fix applied in `scripts/order.py`:
  - Added a `5M` execution guard based on finalized windows rather than single request attempts.
  - The guard looks at the latest `5` finalized `5M` windows where a pair should have been possible and pauses new `5M` entries when `4+` of those windows ended as execution misses.
  - The pause is persisted in `risk-state.json`, logged explicitly, and exposed to runtime state as `executionGuardActive`, so the process stays alive but `record_sample()` skips new entry logic while the flag is active.
  - Validation with synthetic hour summaries confirmed the new guard flips active at `4/5` missed windows.
- The proposed "pre-place both sides at 30 then cancel one later" design is technically possible with the CLOB SDK's limit/cancel APIs, but it is a different strategy with materially higher operational risk:
  - it requires maintaining open GTC orders, order IDs, cancellation guarantees, and heartbeat safety;
  - it reintroduces partial-fill / stale-order risk that the current FOK market-buy path avoids;
  - given the current real-money loss concern, it is safer to keep the reactive threshold-buy design and add the execution guard instead of rewriting into a resting-order strategy right now.
- Local `15M` history currently has `102` valid windows (`durationMinutes >= 13`) backed by `108` raw files / `102` summaries, which is enough for a first-pass replay without browsing external data.
- In this `15M` sample, first-entry triggers happen very early. For a representative `38c` first-leg threshold, all `102` windows hit the trigger with at least `9.34` minutes left, median `13.9` minutes left, so a `5M`-style late-entry cutoff does not add value here.
- Under the same conservative "fill at threshold cap" replay model used earlier, simple equal-dollar `15M` two-leg rules are much weaker than `1H`/`5M`:
  - `35/55` with `$1/$1` is the best equal-dollar pair in the tested grid, but only about `+$1.90` total over `102` windows with max drawdown about `-$7.30`.
  - Most tighter equal-dollar pairs (`30/50`, `35/42`, `38/40`, `40/50`) are still negative on this local sample.
- `15M` becomes more interesting only when the second leg is intentionally oversized to offset the much more expensive hedge fill:
  - `38/55` with `$1` first leg and `$1.5` second leg replayed at about `+$4.91` total with max drawdown about `-$3.56` and max losing streak `2`.
  - More aggressive profit-maximizing settings such as `40/42` with `$1/$2` or `40/55` with `$1/$2` replay higher, but they rely on many trades where the first leg winning is actually a losing outcome, which is operationally harder to reason about.
- A new live `1H` edge case was confirmed on `bitcoin-up-or-down-march-17-2026-2pm-et`: the bot correctly bought the `$2 DOWN` leg at `37c`, but later misclassified a user manual `UP` buy as a bot fill after an exception because the old reconciliation logic only checked "position size increased" and did not verify that the added shares were even plausible for the bot's `$2 at <=38c` order cap.
- The repo now has two separate short-window paper paths:
  - `scripts/order_15m_paper.py` for five rolling `15M` threshold groups
  - `scripts/order_5m_paper.py` for two rolling `5M` threshold groups (`30/30`, `30/35`)
- The new `5M` paper runner records, for every tracked current window, the resolved winner from the window `10` minutes earlier (`REFERENCE_LOOKBACK_MINUTES`, configurable) so later analysis can compare current strategy PnL against a simple prior-window direction context without touching the live order engine.
- The current `4H` rolling paper sample is still small (`8` tracked / `6` resolved), but among the equal-dollar fixed pairs the strongest valid result is `35/35` (`+$3.285715`), while the adaptive `$2` row remains non-comparable because it is still implemented as `$2` per triggered order rather than `$2` per side.
- `scripts/order.py` was already generic enough to support `4H` through the timestamp-start slug mode; the missing pieces for live enablement were only a `4h` default config block, launcher wiring, and order-report/UI variant recognition.
- The paper runners persist their own `rolling/summary.json`, so if an older `order_5m_paper.py` or `order_15m_paper.py` process is still alive after code changes, it will continue overwriting the summary file with the old strategy set until that process is restarted.
- Current paper-settlement context is inconsistent:
  - `scripts/order_5m_paper.py` records a separate reference winner from `REFERENCE_LOOKBACK_MINUTES` (currently default `10` minutes).
  - `scripts/order_15m_paper.py` does not yet track a prior-window reference at all.
  - There is no standalone `4H` paper runner or `run-order-4h-paper.bat` yet.
- Current BAT launchers for `5M` / `15M` paper are plain `python -u ...` wrappers without a window title, which makes multiple paper terminals hard to distinguish operationally.
- The `4h-paper` data path and loader were already wired, but the monitor UI tab list in `app/components/monitor-section.js` still only exposed `5M 下单` and `15M 下单`, so `4H 下单` could not be selected from the page even though the summary file existed.
- A fresh replay on the current rolling `15M` paper sample (`72` tracked / `70` resolved windows from `data/orders/paper-15m/rolling`) shows the currently displayed six groups are all negative, but the local frontier has shifted inside the `35/x` family:
  - `35/36` is the best equal-dollar pair in the current rolling sample at about `-$2.396819`
  - `35/43` and `35/37` are the next best at about `-$3.425253` / `-$3.447869`
  - the user-suggested `35/40` does beat the current on-page leader `35/35` (`-$4.785711` vs `-$4.999995`)
  - `35/45` is worse than `35/40` on this sample (`-$5.285715`)
- So `15M` currently looks better than `5M` operationally, but still does not have a clearly profitable equal-dollar rule on the live rolling sample yet.
# 2026-03-17 5M duplicate-side investigation

- `scripts/order-launcher.js` only supervises child processes for `1H` and `5M`; it does not decide trade direction.
- The actual duplicate-side path is in `scripts/order.py`:
  - `record_sample()` still calls `pick_first_entry_side()` whenever `firstEntrySide` is unset.
  - After a `Request exception`, `place_side_order()` only stores `attemptCount/lastAttemptAt` and may later allow one more `5M` retry.
  - There is no persisted "submit in confirmation" flag that blocks the same side from being selected again before delayed fill verification completes.
- The affected event log `data/orders/logs/btc-updown-5m-1773761700_2026-03-17T15-35-00+00-00.jsonl` shows:
  - `15:36:38Z`: `order-transient-error` on `up` first-entry at `27.0c`
  - `15:36:54Z`: `order-placed` on `up` first-entry at `10.0c`
  - This matches the user's report that the market history ended up with two `UP` buys instead of `UP` then `DOWN`.
- Fix applied in `scripts/order.py`:
  - Added per-order persisted fields: `confirmationPending`, `confirmationStartedAt`, `confirmationBaselineSize`, `confirmationLastCheckedAt`.
  - `can_attempt_order()` now blocks any side that is still in confirmation-pending state.
  - `5M` transient exceptions no longer immediately leave the side retryable; they enter delayed confirmation mode for the existing retry window first.
  - Each sample now runs `refresh_pending_order_confirmations()` before picking a fresh first-entry side.
  - If position size increased, the script marks the original side as filled and sets `firstEntrySide`; if not, it clears the pending lock and only then allows the one configured retry.
- Validation:
  - `compile(..., 'scripts/order.py', 'exec')` succeeded (`syntax-ok`).
  - A local stub test confirmed three branches:
    - delayed fill detected -> `firstEntrySide='up'`, `placed=True`, `confirmationPending=False`
    - no delayed fill after retry gap -> `status='retry-ready'`, `attemptBlocked=False`, candidate can be `up` again once
    - confirmation window not yet elapsed -> `attemptBlocked=True`, `confirmationPending=True`, no candidate returned

# 2026-03-18 settlement claim investigation

- The same-side duplicate-entry confirmation gate is already symmetric for both sides because it is stored per order record (`orders.up` / `orders.down`) and enforced through shared helpers.
- Read-only probing of the current live account returned:
  - `claim_credentials_available() == True`
  - tracked slugs: `53`
  - redeemable tracked entries: `37`
  - claim contracts resolved successfully
  - relayer safe is already deployed
- The current settlement worker is intentionally claim-disabled in code, not blocked by missing credentials:
  - `scripts/auto_redeem.py` sets `claim_work_enabled = False`
  - summary hardcodes `claimApiOnly = True` and `claimDisabled = True`
  - startup log prints `claim=off`
- This explains the runtime screenshot/log mismatch: the worker can see redeemables (`redeemable=36/37`) but never actually calls `order_engine.execute_redeem(...)`.
- Fix applied:
  - `scripts/auto_redeem.py` now enables API claim when `ORDER_AUTO_CLAIM_ENABLED` or `ORDER_AUTO_REDEEM_ENABLED` is true and builder credentials are present.
  - Settlement order is now: auto-sell tracked positions at or above the configured target price first, then API redeem one eligible condition per cycle.
  - Claim success/error is written into summary/log/state so the worker no longer stays in silent detect-only mode.
- Validation:
  - `compile(..., 'scripts/order.py', 'exec')` and `compile(..., 'scripts/auto_redeem.py', 'exec')` both succeeded.
  - A `DOWN`-side stub confirmed the duplicate-order confirmation gate is symmetric: delayed fill on `down` sets `firstEntrySide='down'`, `placed=True`, `confirmationPending=False`.
  - A mocked `auto_redeem.process_once()` run confirmed a successful claim path sets `claimed=True`, clears `afterRedeemableCount` to `0`, stores cooldown state, and remains JSON-serializable even when the raw receipt object is not.
- Monitor threshold aggregate semantics were tightened again: top-level threshold cards must treat each summary as one event, and an event only counts as qualified for `<=30/35/40/45` when both `up` and `down` hit that same threshold inside the same window.
- The `2026-03-18` settlement issue matches a process-liveness gap better than a claim-logic failure: after one auth error there were no further settlement action logs until a manual restart, and the restart immediately claimed the backlog successfully.
- The standalone `5M` paper runner no longer needs to compare only `30/30` and `30/35`; it now also tracks `30/45`, because local user review suggests `30/30` is not attractive enough on recent observations.
- A second independent `15M` paper path now exists for the newer `35/x` set. It reuses `scripts/order_15m_paper.py` through env-configured variant id, output dir, log prefix, and strategy groups, stores data under `data/orders/paper-15m-35x/rolling`, and appears in the monitor UI as `15M 下单2`.
# 2026-03-19 Strategy Analysis

- 当前自适应 `A40/30->40/45($2)` 的投入口径不是“每边总共 $2”，而是“每触发一笔就 $2`。
- 代码位置在 [scripts/paper_adaptive_strategy.py](/d:/cursor/ploymarket/scripts/paper_adaptive_strategy.py)：
  - `build_adaptive_strategy_def()` 默认 `usd_per_order = 2.0`
  - `place_order()` 每一笔都按 `usdPerOrder` 计算 shares
  - `resolve_adaptive_strategy()` 最终把每一笔都累加进 `totalSpentUsd`
- 所以这套策略在多腿窗口里会自然出现平均投入 `>$5` 的结果，这不是 UI 算错。
- 当前 fresh 样本下，固定双腿组合里：
  - `5M` 暂时没有正收益组合，`35/35` 与 `40/40` 只是“最不差”
  - `15M` 里 `40/40` 与 `35/35` 最接近打平
  - `15M 下单2` 里 `35/40` 当前最好，`35/45` 次之
- 扩大到本地历史完整窗口后，固定双腿策略的阈值网格扫描结果更清楚：
  - `5M` 在 792 个窗口上整体仍然为负，最佳区域约为 `36/58~60`，但依然没有正收益。
  - `15M` 在 255 个窗口上出现正收益区，最佳区域约为 `37/52~56`，其中 `37/56` 略优。
  - `1H` 在 87 个窗口上依然为正，`35/37`、`38/38`、`39/42` 都是可行区。
  - `4H` 当前本地只有 17 个完整窗口，样本不足，不适合定正式策略。
- 对 `15M` 的 `37/52`、`37/53`、`37/56` 做“首单 $1、对冲腿提高到 $1.2 / $1.3` 的回放后，结果全部变差：
  - `37/52`: `$1/$1 = +2.53`, `$1/$1.2 = -1.64`, `$1/$1.3 = -3.72`
  - `37/53`: `$1/$1 = +2.85`, `$1/$1.2 = -1.26`, `$1/$1.3 = -3.31`
  - `37/56`: `$1/$1 = +2.88`, `$1/$1.2 = -1.22`, `$1/$1.3 = -3.27`
