# Progress Log

- Created review tracking files.
- Next step: inspect source files and run validation commands.
- Inspected `app/`, `scripts/monitor.js`, config files, and run script.
- Validation completed: lint passed, build passed, monitor script syntax passed.
- Preparing final review with severity-ordered findings and residual risks.
- Implemented fixes in `scripts/monitor.js`, added `lib/monitor-data.js`, added `app/api/monitor/route.js`, and upgraded `app/page.js` to read local monitor data.
- Post-change validation passed: lint, build, snapshot reader, and a short monitor dry run.
- Cleared `data/events` and `data/summaries`, then wired the page and API snapshot to a paginated summary list with date filters.
- New task started: build a standalone order engine, separate from monitoring, for the hourly BTC Up/Down strategy.
- Reviewed the strategy rules and started checking the official Polymarket CLOB order flow before coding.
- Installed `@polymarket/clob-client` and `ethers`, confirmed the SDK supports dollar-denominated market buys, and verified local `axios` connectivity to Polymarket.
- Added `scripts/shared/polymarket.js`, `scripts/order.js`, `.env.order.example`, `run-order.bat`, plus the new `npm run order` script and README documentation.
- Validation passed: `node -c scripts/shared/polymarket.js`, `node -c scripts/order.js`, `npm run lint`, `npm run build`, and dry-run order smoke checks with test artifacts cleaned afterward.
- Investigated `engine.py`, confirmed its Python client auth flow is more robust for this account, then implemented a Python trading engine in `scripts/order.py` and switched `run-order.bat` / `npm run order` to use it.
- Python path validation passed: `python -m py_compile scripts/order.py`, dry-run startup with `ORDER_DRY_RUN=true`, and cleanup of test artifacts under `data/orders`.
- New step: wire the root-level legacy `config.py` into `scripts/order.py` as a fallback source so the new launcher mirrors `engine.py` auth defaults more closely.
- Completed the `config.py` fallback patch and validated that the order engine now loads legacy defaults, selects `signature_type=1`, and resolves the expected funder address during a read-only trader initialization.
- Added settlement reconciliation and JSON report generation under `data/orders/reports`, then validated compile plus report refresh. Current generated totals show `hoursWithOrders=14`, `settledHours=12`, `totalOrders=22`, and `totalNetPnlUsd=2.560127` at validation time.
- Reworked the Next.js home page into a single admin console with tabs for overview, monitor summaries, traded hours, and order-level details; added `app/api/orders/route.js` and `lib/order-data.js`, then validated with `npm run lint` and `npm run build`.
- Investigated threshold tuning over the latest 23 settled monitored hours by replaying local raw event samples and querying settled winners from Polymarket. Compared fixed-dollar thresholds and the current `$1/$2` carry rule to identify which first-entry / hedge pairs sit closer to the local profit frontier.
- Updated the live order defaults and local `.env.order` runtime config from `35/40` to `38/38`, and revalidated with `python -m py_compile scripts/order.py` plus `npm run build`.
- Added a configurable 4-hour monitor mode to `scripts/monitor.js`, exposed monitor timeframe metadata in `lib/monitor-data.js` and the admin UI, added `run-monitor-4h.bat`, and validated with syntax/lint/build plus a short startup probe against the live 4-hour slug pattern.
- Added separate `1H / 4H` sub-tabs to the monitor page by wiring `monitorVariant` filtering through `lib/monitor-data.js`, `app/api/monitor/route.js`, and `app/page.js`, then revalidated with `npm run lint` and `npm run build`.
- Patched the `.env.order` loader in `scripts/order.py` so later non-empty values override earlier blank placeholders, then validated that live order keys now load from the current file layout while relayer/redeem keys are still absent.
- Implemented automatic relayer-based redeem support inside `scripts/order.py`, including alias mapping for raw exported `apiKey/secret/passphrase`, delayed redeem polling for resolved tracked hours, persisted claim status in hourly reports, and documentation updates. Revalidated with `python -m py_compile scripts/order.py`, an import-level config check, and `npm run build`.
- Consolidated monitor startup into a single launcher entry. `run-monitor.bat` and `npm run monitor` now start both the 1-hour and 4-hour monitor processes together via `scripts/monitor-launcher.js`, while `npm run monitor:1h` remains available for the hourly-only path. Revalidated with `node -c scripts/monitor-launcher.js` and `npm run build`.
- Added variant-based monitor defaults so the combined launcher now runs `1H` at `5s` and `4H` at `15s`, and added retention-based cleanup for old raw event files and summaries in `scripts/monitor.js`. Revalidated with `node -c scripts/monitor.js`, `node -c scripts/monitor-launcher.js`, and `npm run build`.
- Reworked the admin `orders` tab from per-order rows to per-hour aggregated settlement rows by grouping `order-details.json` with `hour-details.json` in `lib/order-data.js`, then updated `app/page.js` to show combined direction, trigger chain, total spend, total shares, blended average price, and hour-level PnL. Revalidated with `npm run lint` and `npm run build`.
- Extended monitoring to support BTC `15M` and `5M` timestamp-start variants. `scripts/monitor.js` now has variant defaults for `1H/4H/15M/5M`, the unified launcher starts all four in one BAT, and the monitor page now exposes `1H / 4H / 15M / 5M` tabs. Revalidated with `node -c scripts/monitor.js`, `node -c scripts/monitor-launcher.js`, and `npm run build`.
- Restored the `orders` tab to raw per-order rows, while keeping hourly aggregation only on the hourly summary view. Also shortened displayed hourly windows to a single-line `年月日 + 起止时间段` format (for example `2026/3/16 20点到21点`) in the hour-summary surfaces. Revalidated with `npm run lint` and `npm run build`.
- Tightened short-window monitor validity thresholds to `15M >= 13 minutes` and `5M >= 4.5 minutes`, then revalidated with `node -c scripts/monitor.js` and `npm run build`.
- Reordered the monitor tabs and active-period card to `5M / 15M / 1H / 4H`, bound the monitor header cards to the currently selected monitor variant, removed the extra slug list under the active-period card, and tightened the monitor card layout. Revalidated with `npm run lint` and `npm run build`.
- Added first-hit timestamps and sampling-health metrics to monitor summaries in `scripts/monitor.js`, including longest-gap and estimated-missed-sample tracking.
- Switched monitor retention cleanup to per-variant policies so `5M`/`15M` keep shorter raw histories without pruning older `1H`/`4H` artifacts too early.
- Backfilled first-hit timestamps and sampling-health metrics for older summaries by deriving them from the paired raw JSONL files in `lib/monitor-data.js`, then verified live output with a direct `getMonitorSnapshot({ monitorVariant: '1h' })` probe.
- Validation completed for the monitor enhancement pass: `node -c scripts/monitor.js`, `npm run lint`, `npm run build`, and a direct snapshot read showing populated `firstThresholdHits` plus `samplingHealth`.
- Updated the Python order engine so once both sides are filled for the current hour, it finalizes that trading hour immediately, stops further sampling/trading for that hour, and idles until the next event window. The finalized runtime state is preserved across restarts to prevent duplicate entries before the hour rolls over.
- Revalidated the order engine change with `python -m py_compile scripts/order.py`.
- Deduplicated monitor summary rows in `lib/monitor-data.js` by grouping on variant + event window + slug and keeping the highest-quality sample (duration, sample count, coverage, then recency), so repeated monitor restarts no longer show duplicate rows for the same hour in the admin UI.
- Added a variant-level monitor process lock in `scripts/monitor.js` so opening the same monitor cycle twice now exits the second process instead of writing duplicate raw/summary artifacts.
- Revalidated the monitor dedupe/lock fix with `node -c scripts/monitor.js`, `npm run lint`, `npm run build`, and a direct snapshot probe confirming the repeated `march-16-2026-11am-et` hour now returns exactly one summary row.
- Added a standalone read-only redeem tester in `scripts/test_redeem.py` plus `run-redeem-test.bat`. The tester reuses current order-engine credentials, enumerates redeemable positions, builds claim index sets, runs the dry-run redeem path, and optionally checks relayer readiness without submitting a live claim.
- Verified the tester against the current account and specifically against `bitcoin-up-or-down-march-16-2026-10am-et`; it found the two redeemable legs, built index sets `[1, 2]`, and reported `relayer.ready = true`.
- Added API-driven sell support to the Python trader and expanded `scripts/auto_redeem.py` into a settlement worker that first auto-sells matured BTC strategy positions at a configurable `99.5c` executable threshold, then falls back to browser-based claim handling for truly redeemable positions.
- Live validation executed a real market `SELL` for `bitcoin-up-or-down-march-16-2026-11am-et` (`Down`, `2.82` shares at `0.999`) and another real market `SELL` for `bitcoin-up-or-down-march-16-2026-1pm-et` (`Up`, `6.432126` shares at `0.999`), confirming that the API sell path matches the website's rounded `100美分 / 价值` view closely enough for automation.
- Post-sell validation showed the 1PM position disappeared from `positions`, and balance rose to `$31.340123`.
- Unified settlement scheduling under `scripts/order.py`: the order loop now triggers the settlement worker from one launcher, using `1h` idle scans, `10min` busy scans, and a max of `1` sell per settlement run. Runtime config was enabled in `.env.order` and mirrored in `.env.order.example`.
- Validation passed for the unified launcher path: `python -m py_compile scripts/order.py scripts/auto_redeem.py scripts/claim_portfolio.py`, a safe disabled-mode `order.refresh_auto_redeem(None)` probe, and `npm run build`.
- Added settlement log support to the admin backend: `lib/order-data.js` now reads and compacts `data/orders/redeems/auto-redeem-log.jsonl`, `/api/orders` exposes paginated settlement rows, and `app/page.js` renders a dedicated `结算日志` tab with status badges, balance deltas, sell/claim summaries, and pagination.
- Tightened the settlement worker scope so redeem attempts only track locally recorded strategy hours by default instead of scanning every historical redeemable position in the account.
- Fixed the Playwright browser fallback logger so binary `postData` no longer crashes the request listener, and updated ESLint ignores so `temp/` references plus `data/` runtime artifacts stop breaking project lint runs.
- Validation passed for this UI/logging pass: `python -m py_compile scripts/auto_redeem.py scripts/claim_portfolio.py scripts/order.py`, a Node snapshot probe via `getOrderSnapshot({ settlePage: 1 })`, `npm run lint`, and `npm run build`.
- Re-split operations: removed inline settlement scheduling from `scripts/order.py`, added `run-settlement.bat` plus `npm run settlement`, and turned `scripts/auto_redeem.py` back into the standalone settlement watcher.
- Disabled browser claim execution entirely in `scripts/auto_redeem.py`; settlement is now `API detect + API sell` only and no longer invokes `scripts/claim_portfolio.py`.
- Added an order-engine variant layer so the Python trader can support both `1h` and a dormant `5m` path. The `5m` path now uses timestamp-start slugs, a 5-minute window, and variant-isolated carry lookup.
- Added `ORDER_VARIANT=1h` to live config, updated `.env.order.example`, and added `npm run order:5m` as a dormant command for later activation.
- Validation passed for the split-launcher / dormant-5m pass: `python -m py_compile scripts/order.py scripts/auto_redeem.py` and `npm run build`.
- Ran a conservative replay over the existing `5m` monitor raw files (`222` valid windows). The best-tested combo in this local sample was `30 / 40` with a `1.5` minute late-entry cutoff, ahead of nearby pairs like `32 / 38`.
- Replaced the monitor page's top-right runtime counters with filter-aware threshold aggregate cards (`上侧汇总 / 下侧汇总`) driven by a new `thresholdAggregate` payload in `lib/monitor-data.js`.
- Added a clean standalone monitor UI component at `app/components/monitor-section.js` so the new monitor summary cards and Chinese copy are not blocked by older mojibake text inside `app/page.js`.
- Rewrote `README.md` into a fully Chinese project guide covering monitor, order, settlement, config, retention, and launcher usage.
- Validation passed for this pass: `npm run lint` and `npm run build`.
- Removed the large total number from the top of both threshold summary cards so the cards focus on per-threshold counts and ratios only, then revalidated with `npm run build` and `npm run lint`.
- Re-ran the `5m` local replay on the latest dataset (`255` valid windows) under a flat `$1`/side model with no martingale. Current sample leader is `30/50/1.5m`, ahead of `30/40`, while `35/40` remains much weaker on total profit despite a smoother loss profile.
- Narrowed settlement logging to real settlement actions only: `scripts/auto_redeem.py` no longer appends startup/pending cycles, and the admin data loader now filters out non-settled rows.
- Added a dedicated clean settlement UI component at `app/components/settlement-logs-section.js`, showing actual sold rows with price, shares, proceeds, and balance delta.
- Raised the default auto-sell threshold from `99.5c` to `99.9c` in runtime defaults, `.env.order`, `.env.order.example`, and the Chinese `README`.
- Confirmed again that `5m 30/45` is weaker than both `30/40` and `30/50` in the current flat-$1 replay, so it is not on the local frontier.
- Validation passed for this pass: `python -m py_compile scripts/auto_redeem.py`, `npm run lint`, and `npm run build`.
- Added variant-scoped strategy defaults in `scripts/order.py` so `npm run order:5m` now automatically uses `30/50`, fixed `$1`, `1.5m` late cutoff, and `2000ms` sampling even though live `.env.order` still contains the global `1H` `38/38` settings.
- Added persistent `5M` risk-gate state at `data/orders/risk-state.json`; the order engine now computes rolling resolved PnL from `data/orders/reports/hour-details.json` and pauses new `5M` entries for `8` hours whenever the latest `12` hour window drops to `<= -$10`.
- Hooked the new risk gate into event startup only, so active windows are not interrupted mid-trade; the engine simply delays opening the next `5M` state until the pause clears.
- Rewrote `README.md` in clean Chinese and expanded `.env.order.example` with `ORDER_5M_*` comments plus the new `5M` risk-pause settings.
- Validation passed for the dormant-5M/risk-gate pass: `python -m py_compile scripts/order.py`, `npm run lint`, `npm run build`, plus direct config probes confirming `1H` stays `38/38 + 1/2` while `5M` resolves to `30/50 + fixed $1 + 12h/-10 pause 8h`.
- Added `scripts/order-launcher.js`, changed `run-order.bat`, and switched `npm run order` to a unified launcher that starts `1H + 5M` together with `[1H] / [5M]` prefixed output.
- Changed `scripts/order.py` runtime state persistence from the shared `runtime-state.json` to per-variant files (`runtime-state-1h.json`, `runtime-state-5m.json`) with a legacy read fallback for old `1H` state.
- Extended `lib/order-data.js` so the admin snapshot now derives `summary.byVariant` (`1h`, `5m`) and reads multiple runtime-state files instead of assuming only one running trader.
- Added clean UI replacements in `app/components/order-summary-sections.js`; overview and hour-summary surfaces now show `1H` and `5M`收益分开统计，而订单明细继续混合展示。
- Validation passed for the unified-order/split-summary pass: `python -m py_compile scripts/order.py`, `node --check scripts/order-launcher.js`, `npm run lint`, `npm run build`, plus a direct `getOrderSnapshot()` probe showing `summary.byVariant.1h` and `summary.byVariant.5m`.
- Tightened `scripts/order-launcher.js` so `1H` and `5M` are now truly independent child processes under the same BAT: one child exiting no longer stops the other, and the failed side auto-restarts after `5s`.
- Investigated the new `5M` console noise. The repeated `status_code=400` rows were confirmed to be normal FOK no-fill events, not code crashes; `status_code=None / Request exception!` is the transient CLOB request path.
- Updated `scripts/order.py` so FOK no-fill is logged as `No fill` instead of a hard order error, transient request exceptions get one immediate retry, and the main loop now sleeps against wall-clock elapsed time so `5M` stays close to the intended `2000ms` sample cadence.
- Added a fresh-start gate for `5M`: if the process starts or resumes into the current 5-minute event with less than `4.5` minutes remaining and no order has been placed yet, that window is skipped and the engine waits for the next `5M` event.
- Investigated the user's live `5M` loss report. The actual problem was not four matched fills in one window, but repeated live submission attempts for the same side after request failures within a single `5M` event.
- Patched `scripts/order.py` so `5M` now allows at most one live submission per side per event, removes the in-function immediate retry on `5M`, and checks the actual token position size after an exception before deciding whether the order silently filled.
- Validation passed for the `5M` duplicate-attempt fix: `python -m py_compile scripts/order.py`, a direct `ORDER_VARIANT=5m` probe showing old `runtime-state-5m.json` can no longer retry, and `npm run build`.
- Added a second `5M` guardrail: a per-process minimum submit interval of `4000ms`, so even if direction flips quickly the `5M` engine can issue at most one live order request every 4 seconds while still sampling every 2 seconds.
- Validation passed for the throttling pass: `python -m py_compile scripts/order.py`, a direct `ORDER_VARIANT=5m` config probe showing `sample_ms=2000` and `min_order_interval_ms=4000`, and `npm run build`.
- Investigated the user's settlement console complaint. The worker was not frozen; it was recreating the trader each cycle, printing only auth-probe lines, and staying on the active cadence because `redeemableCount` kept `busy=true` even though API claim execution is disabled.
- Patched `scripts/auto_redeem.py` to reuse one trader instance across cycles, print a compact settlement scan summary each loop, and treat the worker as idle when there are no sell candidates and claim execution is disabled.
- Validation passed for the settlement observability fix: `python -m py_compile scripts/auto_redeem.py`, `python scripts/auto_redeem.py --once`, and `npm run build`.
- Added an explicit settlement startup banner plus `python -u` to `run-settlement.bat` so console output is unmistakably live and unbuffered. Also changed `--once` to print the real next interval instead of the misleading `next=0m`.
- Adjusted the `5M` order gate from “one attempt total” to “at most two attempts per side per event,” but only when the first failure is a verified `transient`/`unfilled` miss. The engine still blocks immediately on success/detected fill and still respects the 4-second submit throttle plus the normal retry cooldown.
- Changed `ORDER_ATTEMPT_COOLDOWN_MS` to a variant-scoped setting so `1H` keeps `15s`, while `5M` now uses `4s`, matching the existing `5M` submit throttle and allowing the one permitted retry to happen before the window is gone.
# 2026-03-17

- Investigated the reported duplicate `UP` order on the `5M` strategy.
- Confirmed the launcher is not the source; the bug is in `scripts/order.py` state handling after transient first-entry exceptions.
- Added a persisted same-side confirmation lock in `scripts/order.py` so a submitted `UP` stays blocked until the engine verifies whether the transient request actually created a position.
- Verified the updated state machine with a local stub test and a syntax-only compile check. `python -m py_compile` was skipped after a locked `__pycache__` file caused a Windows rename error, so validation used `compile()` instead.
- Confirmed the duplicate-order confirmation gate is side-agnostic, so `UP` and `DOWN` both use the same verification path.
- Read-only settlement probing confirmed API redeem prerequisites are available on the current account; the remaining issue is that `scripts/auto_redeem.py` currently runs in detect-only mode and never submits the redeem call.
- Re-enabled API redeem in `scripts/auto_redeem.py` with the intended order of operations: sell first at the configured target, then redeem one eligible tracked condition through the API.
- Verified the updated settlement path with compile checks, a `DOWN`-side duplicate-order stub, and a mocked `process_once()` claim flow that proved summary/log serialization stays safe.
- New `5M` execution issue investigated: the trigger threshold was still correct (`30/50`), but live order submission was blocking the whole `5M` sample loop whenever `place_buy()` hung or returned `Request exception`.
- Implemented non-blocking `5M` submission in `scripts/order.py`: live `5M` orders now enter `submissionPending`, execute on a background executor, and are reconciled by the next sample via `refresh_pending_order_submissions()`.
- Added status visibility for `submitting:*` / `confirming:*`, plus early fill detection while a request is still in flight.
- Validation passed:
  - `compile(..., 'scripts/order.py', 'exec')`
  - local stub showing `place_side_order()` returns immediately, sets `submissionPending=True`, and later resolves to `placed=True` with `firstEntrySide='up'`
- Investigated the recurring live `PolyApiException[status_code=None, error_message=Request exception!]` rows. Confirmed the root issue is the upstream client masking the original `httpx` transport error, not a threshold mix-up.
- Patched `scripts/order.py` to reconfigure the CLOB `httpx` client with explicit timeout settings, default `ORDER_CLOB_HTTP2=false`, and detailed transport-error propagation in `PolyApiException`.
- Expanded settlement from tracked-only to full-account redeemables in `scripts/auto_redeem.py`, added `ORDER_SETTLEMENT_MAX_CLAIMS_PER_RUN`, and updated `.env.order` so the current runtime is set to `tracked_only=false` and `max_claims_per_run=0`.
- Validation passed:
  - `compile(..., 'scripts/order.py', 'exec')`
  - `compile(..., 'scripts/auto_redeem.py', 'exec')`
  - read-only probe confirmed `tracked_only=False`, `max_claims_per_run=0`, and the current account exposes `800` redeemable entries under the widened scope
- Revised after user feedback:
  - Restored settlement runtime config to tracked-only because the full-account redeemable backlog contains too many stale historical positions.
  - Updated `1H` runtime sizing in `.env.order` from `1/2` to `2/3`.
  - Implemented a persistent `5M` execution guard that pauses only new `5M` order branches while keeping the process alive and sampling.
- Validation passed:
  - `compile(..., 'scripts/order.py', 'exec')`
  - `compile(..., 'scripts/auto_redeem.py', 'exec')`
  - read-only probe now confirms `tracked_only=True` and `redeemable_entries=0` for the current tracked-settlement scope
  - a synthetic `5M` guard probe confirmed `evaluate_execution_guard(...) -> active=True` when `4` of the latest `5` finalized windows are modeled as execution misses
- New analysis task started: evaluate whether `15M` is worth testing instead of continuing the unstable `5M` live path.
- Replayed the local `15M` monitor history (`102` valid windows) with a conservative threshold-cap fill model over a grid of first-leg / hedge-leg thresholds and second-leg sizing.
- Current replay takeaway:
  - Equal-dollar `15M` is weak; only wide-hedge pairs like `35/55` are slightly positive.
  - A more balanced candidate exists around `38/55` with `$1` first leg and `$1.5` hedge leg, which improves both total PnL and drawdown versus the equal-dollar alternatives.
  - `15M` first-leg triggers occur very early in the window, so a separate late-entry cutoff does not appear necessary on the current dataset.
- Reset active `5M` profit tracking after the user confirmed the old live `5M` PnL was not trustworthy:
  - archived `34` active `5M` hour-state files, `35` `5M` order logs, and `runtime-state-5m.json` under `data/orders/archive/5m-reset-20260318-022531`
  - rebuilt `data/orders/reports/*` from the remaining active `1H` hour states
  - verified the report layer now shows `summary.byVariant.5m = 0` across hours, orders, spend, payout, and net PnL
- Investigated a live `1H` false-positive pair completion on `bitcoin-up-or-down-march-17-2026-2pm-et`.
- Confirmed from local evidence that:
  - the bot's `$2 DOWN` at `37c` really matched;
  - the later `UP` "detected-after-exception" was actually a manual/external fill because the observed `2.527831` shares imply roughly `79.1c`, which is impossible for the bot's `<=38c` cap order.
- Patched `scripts/order.py` so exception-based fill detection now rejects position increases whose share delta is too small to match the bot's own `amountUsd / priceCap`.
- Also corrected the affected historical hour state/report so that hour now contains only the bot's `$2 DOWN` leg instead of a fake paired `UP`.
- Synced `data/orders/runtime-state-1h.json` to the corrected hour state for `bitcoin-up-or-down-march-17-2026-2pm-et`, so the `1H` worker no longer resumes that stale fake-paired `UP` snapshot after restart.
- Added a standalone rolling `5M` paper runner at `scripts/order_5m_paper.py` plus `run-order-5m-paper.bat`.
- The new `5M` paper path tracks two groups (`30/30`, `30/35`), writes cumulative rolling results under `data/orders/paper-5m/rolling`, and records the resolved winner from `10` minutes earlier alongside each current window for later comparison.
- Changed the monitor top summary cards from per-side counts to event-level qualification counts: `双边达标汇总` now counts only windows where both sides hit the threshold, and `单边未达标` counts windows where either side missed it.
- Added `scripts/settlement-launcher.js` and switched `run-settlement.bat` / `npm run settlement` to a watchdog launcher so the standalone settlement worker now auto-restarts if `auto_redeem.py` exits unexpectedly.
- Expanded `scripts/order_5m_paper.py` from two groups to three groups by adding `30/45`, and widened the console ranking output so all three current `5M` paper groups are visible in the rolling profit log.
- Expanded both paper runners to the same six strategy groups (`30/30`, `30/35`, `30/40`, `30/45`, `35/35`, `40/40`) and added rolling-session backfill logic that can rebuild strategy outcomes from each paper log file.
- Extended `lib/monitor-data.js` and `app/components/monitor-section.js` so the monitor page now has dedicated `5M 下单` / `15M 下单` tabs that read the rolling paper summaries directly.
- Validation passed for this paper-summary pass: Python compile checks, one-shot rolling summary refresh, `getMonitorSnapshot({ monitorVariant: '5m-paper' | '15m-paper' })`, and `npm run build`.
- New paper-runner expansion task started: add a standalone `4H` paper runner, align the prior-window reference rules across `5M` / `15M`, and add Chinese BAT window titles for easier ops.
- Confirmed the baseline before editing:
  - `5M` paper still uses `REFERENCE_LOOKBACK_MINUTES=10`
  - `15M` paper has no prior-window reference state yet
  - there is no `run-order-4h-paper.bat` or `4h-paper` monitor tab yet
- Fixed the missing `4H 下单` tab in `app/components/monitor-section.js` and revalidated with `npm run build`; the monitor page can now navigate to `4h-paper` the same way it already could for `5m-paper` and `15m-paper`.
- Paused the half-specified new adaptive `15M` script idea after the user changed direction mid-design, removed the unfinished file, and instead replayed the current rolling `15M` dataset for the more relevant `35/x` family.
- New replay result on the current rolling sample: `35/36` now ranks best among equal-dollar `15M` pairs, while `35/40` is slightly better than the currently displayed `35/35` and `35/45` is slightly worse.
- Refactored `scripts/order_15m_paper.py` so one engine can drive multiple independent `15M` paper sessions via env-configured variant id, output dir, log prefix, and strategy groups.
- Added `run-order-15m-paper-2.bat` for the separate `35/36`, `35/40`, `35/45` rolling test.
- Wired that second rolling summary into the admin UI as `15M 下单2` and revalidated with `python -m py_compile scripts/order_15m_paper.py` plus `npm run build`.
# 2026-03-19

- Rebuilt all paper sessions from zero after archiving prior rolling directories to `data/orders/archive/paper-reset-20260319-183935`.
- Added a reusable adaptive paper strategy helper in `scripts/paper_adaptive_strategy.py`.
- Wired the adaptive `$2` strategy into `5M / 15M / 15M-35x / 4H` paper runners and added a new `1H` paper runner at `run-order-1h-paper.bat`.
- Verified Python syntax and Next.js production build after the paper-runner changes.
- Ran a broad historical threshold scan across local monitor history and saved results to `data/orders/archive/strategy-scan-20260319.json`.
- Key scan result:
  - `5M` best fixed pair on larger history is still negative (`36/59`).
  - `15M` has positive fixed pairs around `37/52` to `37/56`, with `37/56` slightly best on the current larger sample.
  - `1H` remains positive with `38/38`, and `35/37` / `39/42` are close alternatives.
  - The adaptive `$2` strategy is currently over-sized because it spends `$2` per triggered order, not `$2` per side.
- Added a third dedicated `15M` paper runner (`run-order-15m-paper-3.bat`) for the `37/52`, `37/53`, `37/56` family, wired it into the monitor data/tab config as `15M 下单3`, rebuilt the Next.js app successfully, and started the new rolling session under `data/orders/paper-15m-37x/rolling`.
- Added live `4H` order-engine support by wiring a new `4h` variant into `scripts/order.py`, extending `scripts/order-launcher.js` so `run-order.bat` now supervises `1H + 4H`, and updating the order-report data/UI layer to recognize and summarize `4H` separately from `1H`.
- Validation for the `4H` live integration:
  - `node --check scripts/order-launcher.js` passed
  - source-level `compile()` of `scripts/order.py` passed
  - `ORDER_VARIANT=4h` probe resolved `btc-updown-4h-`, `240m`, `35/35`, `$1`, `30000ms`
  - `npm run build` passed
- Normalized the adaptive paper strategy to `$1` per triggered order as well, so all paper simulations now use `$1` instead of mixing fixed `$1` legs with an adaptive `$2` branch.
